## TCP Port Checks — Mailcow Mail Ports
## Managed by Ansible — do not edit manually
## Generated: {{ ansible_facts['date_time'].iso8601 }}

init_config:

instances:
  - min_collection_interval: {{ dd_min_collection_interval }}
    name: smtp_25
    host: localhost
    port: 25
    timeout: 5
    tags: [env:{{ dd_env }}, role:{{ server_role }}, host:{{ ansible_facts['hostname'] }}, service:mail, protocol:smtp]

  - min_collection_interval: {{ dd_min_collection_interval }}
    name: smtp_587
    host: localhost
    port: 587
    timeout: 5
    tags: [env:{{ dd_env }}, role:{{ server_role }}, host:{{ ansible_facts['hostname'] }}, service:mail, protocol:smtp_submission]

  - min_collection_interval: {{ dd_min_collection_interval }}
    name: smtps_465
    host: localhost
    port: 465
    timeout: 5
    tags: [env:{{ dd_env }}, role:{{ server_role }}, host:{{ ansible_facts['hostname'] }}, service:mail, protocol:smtps]

  - min_collection_interval: {{ dd_min_collection_interval }}
    name: imaps_993
    host: localhost
    port: 993
    timeout: 5
    tags: [env:{{ dd_env }}, role:{{ server_role }}, host:{{ ansible_facts['hostname'] }}, service:mail, protocol:imaps]

  - min_collection_interval: {{ dd_min_collection_interval }}
    name: pop3s_995
    host: localhost
    port: 995
    timeout: 5
    tags: [env:{{ dd_env }}, role:{{ server_role }}, host:{{ ansible_facts['hostname'] }}, service:mail, protocol:pop3s]
